import { EcommerceValidadores } from './ecommerce-validadores';

describe('EcommerceValidadores', () => {
  it('should create an instance', () => {
    expect(new EcommerceValidadores()).toBeTruthy();
  });
});
